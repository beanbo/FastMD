# FastMD: выбор технологического стека

> Итоговый отчёт исследовательской фазы, 19.09.2026.
>
> - **Теория:** 6 отчётов в [`research/`](research/).
> - **Практика:** 12 прототипов и 47 вариантов в [`bench/protos/`](bench/protos/). Все измерены одним харнессом по одному протоколу ([`bench/PROTOCOL.md`](bench/PROTOCOL.md)).
> - **Проверки:** у каждого прототипа был независимый аудит честности замера и картинки. Сам отчёт прошёл состязательную проверку трёх критиков: все 47 × 3 медианы пересчитаны из сырых данных, интерпретации исправлены по их замечаниям.

---

## 0. Коротко

**Рекомендация: C++ + Win32 + DirectWrite + md4c со своим движком отрисовки документа.**
- Первый кадр считается на CPU, без GPU-устройства.
- Сначала отрисовывается только первый экран, остальное дорисовывается потом.

Этот путь даёт максимальный запас скорости и лучший вид. Платить придётся самым большим объёмом ручной работы: выделение текста, поиск, копирование, доступность и подмножество HTML пишутся с нуля (§6–7). Если цена окажется неприемлемой, запасные пути определены заранее (§7, §9).

| Что открываем | Время до отрисованного документа, medium.md (53 КБ), медиана |
|---|---|
| **Прототип FastMD `cpp-d2d`** | **59 мс** |
| `cpp-d2d` в резидентном режиме (процесс уже запущен) | **28 мс** |
| Пустое Win32-окно (эталон без приёмов ускорения) | 78 мс |
| Блокнот Windows 11 (режим синтаксиса, без форматирования; восстанавливает вкладки сессии) | 473 мс |
| WinUI 3, лучшая измеренная конфигурация (C#, NativeAOT) | 324 мс |
| WebView2 / wry / Tauri | 447–630 мс |
| Electron (класс Typora, Obsidian, MarkText) | 753–903 мс |
| VS Code (только окно редактора, не превью) | 1 138 мс |
| **Void — ваше текущее приложение для .md** | **1 444 мс**. Это только до окна: на чистом профиле документ закрыт онбордингом, а с вашим профилем и расширениями, вероятно, дольше |
| Cursor | 2 351 мс |

Про 59 мс: это уровень «настроенного пола». Пустое окно с теми же приёмами (отключённая инициализация IME для потока, показ без активации) даёт ≈58–64 мс ([05 §5](research/05-windows-startup-engineering.md)). Эталон 78 мс этих приёмов не использует. Иначе говоря, Markdown на критический путь почти не попадает.

**Главное:**

- **Время уходит на инициализацию платформы, а не на Markdown.** md4c разбирает 3,7 МБ за 10,6 мс. Остальное — GPU-драйвер, браузерные процессы, рантаймы и первая активация окна.
- **На ядре GFM «стильно» получается почти везде.** Нативный рендер на DirectWrite по раскладке совпадает с эталоном на Chromium (9/10, §4). Оговорка: корпус не содержал сырого HTML, SVG-бейджей и удалённых картинок, а они есть в 68–85 % README на GitHub. Веб-движки рисуют их бесплатно, нативному движку их предстоит написать.
- **Среди стеков быстрее 150 мс выбор — это запас скорости против объёма работы:**

  | Стек | medium.md |
  |---|---|
  | cpp-d2d | 59 мс |
  | RichEdit | 69 мс |
  | litehtml | 103 мс |
  | Blitz | 112 мс |
  | Avalonia | 143 мс |
  | Qt | 149 мс |

  Все они укладываются в цель продукта «≤250 мс» из [06](research/06-product-ux-landscape.md). `cpp-d2d` выбран потому, что главная цель — «пуля»: ≈86 мс от двойного клика против ≈170–180 мс у Qt и Avalonia. Цена — самая большая трудоёмкость (§7).
- **WinUI 3 открывается в 5,5 раза медленнее.**
  - Около 135 мс из 324 (≈40 %) он ждёт D3D11-устройство для XAML-композитора на NVIDIA. Само устройство стоит ≈200 мс, но создаётся параллельно с окном.
  - Ещё ≈80 мс уходит на фиксированную инициализацию XAML.
  - Ни одна непроверенная «ручка» не опустит его ниже ≈260–290 мс (§5).
- **Веб-движок нужен только как ленивый «богатый режим»** для Mermaid и полного KaTeX. У WebView2 нижний предел ≈0,45 с на каждый новый процесс, и документ с флагами его почти не сдвигают.

---

## 1. Как мерили

**Машина:**
- Windows 11 Pro 26200, Ryzen 9 7950X (16 ядер), 64 ГБ, NVMe;
- RTX 5070 + AMD iGPU, монитор 165 Гц;
- Defender RTP включён;
- во все процессы внедряются Windhawk и RTSS: хук RTSS стоит ≈10 мс на первое окно каждого процесса, включая эталон.

**Метрика `t_content`.** Отсчёт идёт от вызова `CreateProcessW` до момента, когда первый экран отрисованного и оформленного документа попал на экран. Этот момент приложение фиксирует само: вызывает `DwmFlush` и записывает точное время. У веб-движков отметка ставится после двойного `requestAnimationFrame` плюс `DwmFlush` у хоста.

**Как проверяли отметку.** Харнесс ([`bench/harness/bench.py`](bench/harness/bench.py)) запускает приложение в Job Object, поэтому учитывает и дочерние процессы WebView2/Electron, и независимо следит за появлением окна. Аудиторы дополнительно снимали экран пиксельными зондами:

| Стек | Когда документ реально на экране относительно отметки |
|---|---|
| GDI/DIB: `cpp-d2d`, RichEdit, Qt, WPF, wry/Tauri | в том же кадре композиции |
| XAML и WebView2 без GPU-процесса | на 1–4 кадра позже (≈6–35 мс) |
| Веб-варианты с отдельным GPU-процессом | на 15–95 мс позже |
| egui-softfirst | отметка ставится внутри `ShowWindow`, ≈10 мс раньше стеков, отмечающих в `WM_PAINT` |
| Avalonia, iced | отметка консервативна на 6–50 мс |

Почти все эти погрешности идут в пользу медленных стеков, поэтому вывод они не меняют.

**Корпус.** Кириллица, CJK, цветные эмодзи, GFM-таблицы, задачи, вложенные списки, код на 7 языках, локальные картинки.

| Документ | Размер | Строк |
|---|---|---|
| small.md | 3,4 КБ | ~90 |
| medium.md | 53 КБ | 1,4 тыс. |
| large.md | 3,7 МБ | 97 тыс. |

**Финальный прогон:**
- 45 вариантов × 3 документа × 13 запусков в чередующемся порядке; 2 резидентных варианта отдельными прогонами подряд;
- первый запуск в медиану не входит;
- прогон шёл через контекст explorer.exe, чтобы не влияла виртуализация AppData/HKCU от MSIX-пакета Claude Desktop. Сверка с прогоном из сессии: нативные стеки медленнее на 2–2,5 мс, тяжёлые на 5–10 %;
- сырые данные: [`bench/results/final/`](bench/results/final/).

**Оговорки:**

1. **Все замеры сделаны на тёплом файловом кэше.** exe, DLL, шрифты и профили WebView2/Electron уже в памяти. Настоящий холодный старт (после перезагрузки, первый запуск после установки или обновления) не мерился. Он сильнее бьёт по стекам с большими бинарниками: WinUI (235 файлов), Electron, WebView2 Runtime.
2. **Сторонние приложения (`ext-*`) не сообщают о контенте.** Для них измерено только появление окна с именем файла в заголовке, так что реальное время ещё больше.
3. **Приёмы ускорения применены неравномерно.**
   - Отключение IME есть в cpp-d2d, RichEdit, Qt, litehtml, Sciter, WPF и egui-noime.
   - Его нет в WinUI, Avalonia, Blitz, iced, egui-softfirst и веб-стеках.
   - Это ≈10–15 мс в пользу первых, на этой машине больше: `TextInputHost.exe` набрал 139 тыс. секунд CPU по Get-Process на 19.09 и перед прогоном не перезапускался.
   - **Разницы до ≈15 мс между этими группами не показательны.**
4. **`t_content` не включает анимацию открытия окна Windows 11.** У быстрых стеков документ полностью виден через ≈40–80 мс после `t_content`. У стеков, которые показывают окно заранее (WinUI, WebView2, Electron), анимация успевает закончиться до появления контента. `cpp-d2d` отключает анимацию (`DWMWA_TRANSITIONS_FORCEDISABLED`), но это один вызов, доступный любому стеку с HWND. Это не преимущество стека, а **обязательный пункт при любом выборе**.
5. **Хвост распределения.** 14 из 1 833 запусков (у эталона 3 из 39) простояли 2,1–2,7 с внутри самого `CreateProcessW`, примерно раз в круг прогона. Медиана и p90 это скрывают. От стека это не зависит; избежать можно только без создания процесса (§6, резидент).
6. **Цифры относятся к машине с дискретной NVIDIA и 16-ядерным CPU.**
   - На ноутбуке только с iGPU штраф за GPU-устройство меньше: здесь AMD iGPU стоит ≈55–85 мс против ≈205 мс у NVIDIA.
   - CPU-растр на слабом CPU будет медленнее.
   - На мониторе 60 Гц `DwmFlush` ждёт до 16,7 мс.

   Порядки величин от этого не меняются, но до финального решения стоит повторить замер на слабом ноутбуке (§9).

---

## 2. Результаты: лучший вариант каждого стека

Медиана `t_content` в мс по 12 запускам (первый исключён).

| Стек, лучшая честная конфигурация | small | **medium** | large | Размер дистрибутива | Визуал (аудит) |
|---|---|---|---|---|---|
| **C++ Win32 + DirectWrite → GDI DIB + md4c** (`cpp-d2d`) | 60 | **59** | 61 | 0,4 МБ | **9** |
| C++ RichEdit windowless + md4c→RTF (`cpp-richedit-wl-mt`) | 70 | **69** | 72 | 0,26 МБ | 7 |
| C++ RichEdit, обычный контрол (`cpp-richedit`) | 80 | **84** | 86 | 0,25 МБ | 7 |
| egui, первый кадр на CPU, пропатченный winit (`rust-gpu-egui-softfirst`)¹ | 78 | **77** | 83 | 7,5 МБ | 7 |
| *Эталон: пустое Win32-окно без приёмов ускорения* | 77 | *78* | 78 | 0,15 МБ | — |
| litehtml + свой контейнер D2D/DWrite, CPU (`litehtml-sciter-litehtml`) | 90 | **103** | 102 | 1,8 МБ | 9 |
| Blitz: Rust, настоящий CSS через Stylo, CPU-растр (`blitz`) | 111 | **112** | 115 | 8 МБ | 8 |
| iced 0.14, tiny-skia CPU (`rust-gpu-iced-tinyskia`) | 120 | **129** | 183 | 7,8 МБ | 6,5 |
| Avalonia 12 NativeAOT, Skia CPU (`avalonia`) | 132 | **143** | 143 | 31 МБ | 8 |
| Qt 6.12 Widgets, статический (`qt`) / официальные DLL (`qt-dll`) | 141 / 150 | **149 / 154** | 151 / 160 | 20 / 27 МБ | 8,5 |
| WPF .NET 8 + R2R + нативный лаунчер (`wpf`) | 295 | **287** | 295 | 1,75 МБ + .NET | 8 |
| Sciter.JS 6 | 319 | **320** | 326 | 19 МБ | 7 |
| **WinUI 3, C# NativeAOT** (`winui3`) | 302 | **324** | 330 | 58 МБ (8 МБ без встроенного WinAppSDK) | 8 |
| C++ + WebView2 с тюнингом (`cpp-webview2`) | 429 | **447** | 445 | 0,5 МБ | 9 |
| Rust wry + WebView2 | 468 | **473** | 474 | 2,5 МБ | 9 |
| WinUI 3 + WebView2 | 479 | **497** | 495 | 58 МБ | 9 |
| Tauri 2 | 591 | **630** | 668 | 5,6 МБ | 9 |
| Electron 44 с тюнингом | 727 | **753** | 744 | 324 МБ | 9 |
| **Резидентные режимы**² | | | | | |
| `cpp-d2d-resident` | 29 | **28** | 29 | 0,4 МБ | 9 |
| `cpp-webview2-resident` | 65 | **65** | 84 | 0,5 МБ | 9 |

¹ Возможен только с патчами winit и wgpu и своим CPU-растром; метка ≈10 мс раньше, чем у стеков, которые ставят её в `WM_PAINT` (§1).
² Процесс уже запущен, лаунчер из того же exe передаёт путь через `WM_COPYDATA`. Это не DelegateExecute, см. §6. Первый документ после старта резидента WebView2 открывается за 165–280 мс; в медиану он не вошёл.

**Большой файл.** Если рендерить весь документ до первого кадра, время на large.md такое:

| Вариант | Весь документ сразу | Первый экран первым |
|---|---|---|
| RichEdit, оконный | 998 мс | 86 мс |
| Qt | 721 мс | 151 мс |
| Blitz | 2 478 мс | 115 мс |
| WebView2: без тюнинга, с GPU-процессом / с тюнингом | 2 105 мс | 445 мс |

**Вывод: «первый экран первым» обязателен для любого стека.**

Полная таблица всех 47 вариантов — в приложении A.

---

## 3. Куда уходят миллисекунды: законы этой машины

### 3.1 Аппаратное GPU-устройство: ≈150–250 мс, а с пайплайнами шейдеров до +620 мс

На NVIDIA `D3D11CreateDevice` тянет ≈195 МБ пользовательского драйвера и проверку подписи ([05 §4](research/05-windows-startup-engineering.md)). Тот же код без GPU и с GPU:

| Прототип | Без GPU | С GPU | Разница | Примечание |
|---|---|---|---|---|
| cpp-d2d | 59 | 261 | +202 | |
| litehtml | 103 | 259 | +156 | |
| Avalonia | 143 | 344 | +201 | |
| egui | 77 | 324 | +247 | |
| Blitz | 112 | 735 | +623 | плюс D3D12-пайплайны vello_hybrid |

В `cpp-d2d-gpu` устройство создаётся в рабочем потоке с ~26-й по 206-ю мс, то есть ≈180 мс. UI-поток ждёт его с 52-й мс.

Кто платит этот налог:
- **Обязательно:** WinUI 3 (XAML-композитор всегда берёт аппаратный адаптер), WPF (D3D9), Avalonia с ANGLE, wgpu, `ID2D1HwndRenderTarget` по умолчанию.
- **Chromium, WebView2, Electron — только в конфигурации по умолчанию.** Отдельный GPU-процесс стартует параллельно и добавляет 18–55 мс: `cpp-webview2-default` 465 против 447 мс, `electron-gpu` 808 против 753 мс. Лучшие веб-варианты работают без GPU-процесса. Их медленность — это пол в ≈0,45 с (§3.3).

**Для своего движка: первый кадр рисуется на CPU.** DirectWrite → GDI DIB, D2D на WARP или Skia CPU.

### 3.2 Налог на размер EXE (вероятнее всего, Defender): ≈1,2–1,9 мс на каждый МБ, на каждом запуске

Время уходит на синхронную обработку в ядре внутри `CreateProcessW`. По Blitz получалось до ~2,2 мс/МБ ([05 §2.2](research/05-windows-startup-engineering.md)).
- **Electron:** до первой строки кода проходит **461 мс** (`launch_overhead`, exe 235 МБ).
- **VS Code и Cursor:** ≈250 мс на каждый процесс.
- **.NET single-file 64 МБ:** +130 мс.

DLL этого налога не платят. **Правило: маленький exe, тяжёлое — в DLL.**

### 3.3 Пол веб-движка: ≈0,45 с на новый процесс

Разбивка `cpp-webview2`:

| Интервал, мс | Длительность | Что происходит |
|---|---|---|
| 0 → 42 | 42 | exe стартовал, вызвано создание среды |
| 42 → 290 | ≈240 | подъём browser-процесса, контроллер готов |
| 290 → 432 | ≈140 | renderer-процесс, навигация, DOM |
| 432 → 441 | ≈10 | первый кадр |

Флаги Chromium (без GPU-процесса, сеть внутри browser-процесса) дают 20–60 мс и снижают CPU на треть, но пол не сдвигают. Tauri прибавляет к голому wry ≈150 мс: окно tao активируется последовательно и строит кэш раскладки клавиатуры. Подробно — [02 §15](research/02-web-and-html-engines.md) и README прототипов.

### 3.4 Первая активация окна

- **TSF/IME при первом `ShowWindow`** стоит 12–35 мс. Лечится вызовом `ImmDisableIME(0)` только для UI-потока документа, как в прототипе. `ImmDisableIME(-1)` выключает IME во всём процессе навсегда, и ввод на CJK в поиске станет невозможен. Альтернатива — показ без активации и активация после первого кадра (не мерилась из Explorer).
- **Анимация открытия DWM** прибавляет ≈40–80 мс после отрисованного кадра. Лечится `DWMWA_TRANSITIONS_FORCEDISABLED`.
- **Кэш раскладки клавиатуры в winit и tao** (Rust-тулкиты, Tauri) стоит 150–350 мс в первом `ShowWindow`. Лечится только патчем winit.

### 3.5 Рантаймы — не главная проблема, главная — фреймворки поверх них

- **Сам .NET:** NativeAOT добавляет ≈4 мс к нативному минимуму, CLR с JIT — ≈15 мс ([05 §7](research/05-windows-startup-engineering.md)).
- **WinUI 3:** от `main` (57 мс) до `activate` (137 мс) идёт инициализация XAML; дальше до `loaded` (270 мс) — ожидание устройства и композитора.
- **WPF:** рендер-поток и D3D9. Прототип добрался до 287 мс только с нативным лаунчером, предзагрузкой D3D9 и без темы Aero2; вариант «как в учебнике» — 487 мс.

### 3.6 Что не влияет или не измерено

- **Парсер не влияет:** md4c разбирает 3,7 МБ за 10,6 мс, pulldown-cmark — за 16,8 мс ([04](research/04-markdown-pipeline.md)).
- **Статический или динамический CRT, манифест:** ±2 мс.
- **Подпись кода не измерялась.** По [05 §2.2](research/05-windows-startup-engineering.md) известный мелкий exe создаётся за 4–6 мс против 18–24 мс у нового неподписанного. Подпись и репутация могут дать до ~15 мс.
- **MSIX напрямую не измерялся** (нет Developer Mode). Косвенно: виртуализация AppData замедляет файловый I/O в 1,3–2,4 раза. Выигрыша в скорости не ожидается.

### 3.7 Бюджет «пули» (`cpp-d2d`, medium.md)

| Отметка, мс | Что готово |
|---|---|
| 17 | `CreateProcessW` вернул управление |
| 29 | `main` |
| 30 | файл прочитан и разобран md4c |
| 31 | DirectWrite готов |
| 40 | вёрстка первого экрана |
| 41 | окно создано |
| 43 | кадр отрисован в памяти |
| 51 | окно показано |
| 52 | кадр скопирован в окно (BitBlt) |
| **59** | **`DwmFlush`: кадр на экране** |

Собственный конвейер документа (чтение, md4c, DirectWrite, вёрстка и рендер первого экрана) занимает ≈14 мс и идёт параллельно с созданием окна. **Критический путь после `main` — вызовы ОС:**
- `CreateWindowEx` с внедрёнными хуками: ≈11 мс, из них RTSS ≈10;
- `ShowWindow`: ≈8–12 мс;
- ожидание композиции DWM: ≈7 мс.

Резервов внутри приложения почти не осталось.

---

## 4. Визуальное качество

Скриншоты medium.md — в [`bench/results/final/shots/`](bench/results/final/shots/). Вот как стеки распределились по оценке аудита:

- **9/10.** Нативный `cpp-d2d` по раскладке совпадает с эталоном Chromium: заголовки на тех же Y-координатах, таблицы, цветные эмодзи, CJK. Чекбоксы задач у него аккуратнее, чем у Chromium. На том же уровне litehtml, WebView2, Electron и wry — у них общий `style.css` в духе GitHub.
- **8–8,5/10.** Qt, WinUI 3, Avalonia, WPF, Blitz.
  - У WinUI 3, Avalonia и WPF нет скругления у inline-кода, а выделение работает только внутри одного блока.
  - В WPF эмодзи монохромные.
  - У Blitz нет подсветки кода.
- **7/10.** RichEdit: монохромные эмодзи, ссылки всегда подчёркнуты, нет скруглений. egui: сглаживание в градациях серого без ClearType, монохромные эмодзи, синтетический курсив.
- **6,5/10.** iced: знаки препинания после жирного фрагмента или inline-кода наследуют его стиль.

Вывод: **на ядре GFM выбор по скорости не стоит ничего в виде.** Сырой HTML, SVG-бейджи и удалённые картинки корпус не содержал, и они не проверялись. Нативному движку их предстоит написать (§6).

Замечание о типографике. Спецификация бенчмарка (колонка 860 DIP, текст 15 DIP) даёт ≈119 символов в строке. Для продукта [06 §10.1](research/06-product-ux-landscape.md) советует текст 16 DIP и колонку до 680 DIP, а таблицы, код и картинки делать шире колонки.

---

## 5. Отдельно про WinUI 3

Для C#-пути проверены основные «ручки»:
- NativeAOT;
- self-contained WinAppSDK 1.8;
- свой `Main`;
- документ собирается в коде, без `XamlControlsResources`;
- первый экран первым;
- разбор начала документа отдельно от остального.

| Вариант | medium.md |
|---|---|
| Лучший (`winui3`) | **324 мс** |
| JIT + R2R | 384 мс |
| С Fluent-ресурсами и Mica | 449 мс |
| С WebView2 внутри | 497 мс |

**Почему быстрее не будет:**

1. **Композитор ждёт GPU-устройство ≈135 мс** на критическом пути, между `activate` (137 мс) и `loaded` (270 мс). Само устройство на NVIDIA стоит ≈200 мс, но частично создаётся параллельно. Публичного способа перевести XAML на WARP нет: строки `ForceWarp` и `SoftwareRendering` в `Microsoft.UI.Xaml.dll` есть, API нет.
2. **До `Activate` WinUI тратит 137 мс ещё до ожидания GPU.** Это вдвое больше, чем весь путь `cpp-d2d`.
3. **Mica в WinUI стоит +125…240 мс**, потому что `SystemBackdrop` создаёт GPU-устройство синхронно. В чистом Win32 тот же Mica через `DwmSetWindowAttribute(DWMWA_SYSTEMBACKDROP_TYPE)` почти бесплатен ([05 §5](research/05-windows-startup-engineering.md)).
4. **Дистрибутив 58 МБ** в self-contained-сборке. Если опираться на установленный Windows App Runtime 1.8, получается 8 МБ при той же скорости.

**Не проверялось:**

| Ручка | Ожидаемый эффект |
|---|---|
| C++/WinRT вместо C# | ≈−30 мс до `main` |
| `ImmDisableIME` или активация после кадра | ≈−10…20 мс |
| GPU-предпочтение iGPU для приложения | вряд ли поможет: на этой машине кросс-адаптерная цепочка на iGPU сама стоит ≈190 мс (`cpp-d2d --igpu`) |
| Резидентный режим | оценка автора прототипа 100–150 мс |

**Даже если сработает всё, холодный старт — ≈260–290 мс. Вывод не меняется.**

**Трудоёмкость.** По оценкам прототипистов MVP сопоставим с `cpp-d2d` (6–8 недель). Но до полноценного продукта WinUI ближе: 12–14 недель против «6–8 недель + 3–4 месяца» у DirectWrite. Разница — в основном доступность (UIA) и готовые контролы. RichTextBlock даёт выделение через абзацы, но не вмещает таблицы и блоки кода без InlineUIContainer и не умеет виртуализацию. Мы платим этой разницей за пятикратный выигрыш на старте.

**Вердикт:** для отрисовки документа WinUI 3 не брать. Его можно использовать только для «богатой обвязки» (настройки, диалоги), которая подгружается после первого кадра через XAML Islands, как в Блокноте Windows 11. Но нативный Win32 с Mica через DWM и так выглядит как Windows 11, поэтому в v1 это не нужно.

---

## 6. Рекомендуемая архитектура FastMD

```
fastmd.exe  (C++20, Win32, /SUBSYSTEM:WINDOWS, static CRT, /GS + CFG в релизе, < 2 МБ; редкое — delay-load DLL)
 ├─ старт: ImmDisableIME(0) только для UI-потока документа (поиск с IME — отдельный поток или лениво);
 │         DWMWA_TRANSITIONS_FORCEDISABLED; Mica/тёмный заголовок через DWM; без COM/WinRT на критическом пути
 ├─ парсинг: md4c в UTF-16 (свой форк, закреплённый коммит master 0.6.0-dev ради сносок и Alerts;
 │           до перехода проверить UTF-16-сборку и регрессию автоссылок; в CI — CommonMark + GFM-spec)
 ├─ модель: блоки + inline-прогоны; у блока свой IDWriteTextLayout; LRU-кэш вёрсток (все вёрстки large.md = +1,5 ГБ)
 ├─ отрисовка: ОДИН программный бэкенд — DirectWrite → GDI DIB (как в прототипе). Замер прокрутки прототипа:
 │            GDI 2,1 мс/кадр, WARP 2,7, аппаратный D2D 11,1 (1000×800); при 3300×1300 — 6,4 / 3,9 / 9,0.
 │            Второй бэкенд (D2D WARP/HW) — только если замер на слабом ноутбуке/4K покажет нехватку
 ├─ вёрстка: только первый экран до первого кадра; остальное — в фоне, высоты сначала оценочные
 ├─ подсветка: Lexilla (лексеры Scintilla): 0,1–0,3 мс инициализации, 22–36 МБ/с — синхронно
 ├─ эмодзи: послойный COLR через TranslateColorGlyphRun, как в прототипе (59 мс уже с цветными эмодзи на экране)
 ├─ картинки: WIC — размер синхронно (0,1 мс), декодирование асинхронно; удалённые — асинхронно, с дисковым кэшем
 │            и настройкой приватности; SVG с текстом — lunasvg или resvg
 ├─ HTML-подмножество (06 §4.3: img/align/width, p/div align, br, details/summary, picture/prefers-color-scheme,
 │   sub/sup/kbd, простые table) — одно из двух: (а) свой мини-DOM внутри движка; (б) litehtml как движок ВСЕГО
 │   документа (прототип: 103 мс, 9/10). Не «litehtml только для HTML-блоков»: в README HTML чаще inline или
 │   оборачивает Markdown, а два движка в одном документе — это раздельные выделение и поиск
 ├─ math/mermaid: v1 — как оформленный код; v1.x — RaTeX (≈0,07 мс/формула) и mmdr (≈40–90 мс) в фоне;
 │                запасной путь — WebView2 → SVG-кэш (Mermaid с htmlLabels:false; окно cloaked, а не скрытое —
 │                иначе не срабатывают rAF/getBBox; virtual host только в зоне .example/.invalid;
 │                один экземпляр WebView2 на процесс)
 └─ резидент + DelegateExecute — только если замер на слабом ноутбуке даст холодный двойной клик > 150 мс
```

RaTeX, mmdr и resvg написаны на Rust. C++-продукту для них понадобятся cargo и FFI (или lunasvg плюс WebView2). Это аргумент рассмотреть Rust для всего проекта (§7).

**Бюджет старта:**
- ≈60 мс до первого экрана при запуске нового процесса на тёплом кэше;
- от двойного клика ≈86 мс, потому что Explorer добавляет ≈27 мс;
- резидент через DelegateExecute сократил бы это до ≈35–40 мс. Оценка по тривиальному вьюеру из [05 §8.2](research/05-windows-startup-engineering.md); 28 мс в §2 измерены через `--forward`, а не через DelegateExecute. Это ниже порога восприятия, и [06 §7.3](research/06-product-ux-landscape.md) считает резидент для нативного пути ненужным.

**Функции v1** (по [06 §10.1](research/06-product-ux-landscape.md)):
- **рендер:** GFM, alerts, сноски, front matter, emoji-шорткоды, HTML-подмножество;
- **картинки:** локальные, удалённые и SVG с текстом;
- **чтение:** поиск, оглавление, зум, выделение и копирование, копирование кода;
- **навигация:** якоря, переход по .md с «Назад»;
- **окружение:** live reload с сохранением прокрутки, тема по системе (у вас тёмная), «Открыть в редакторе», ассоциация .md, drag-and-drop, Jump List.

**Сознательно вне v1:** RTL/bidi-документы, переносы слов, печать и PDF.

**v2:** нативный preview handler для панели Проводника. Превью PowerToys открывается за 0,6–1,4 с; мы можем быть в 5–10 раз быстрее (оценка ≈50–150 мс, не измерено).

**Трудоёмкость** (оценки прототипистов, не откалиброваны):
- MVP на базе `cpp-d2d` ≈6–8 недель: выделение и копирование, поиск, зум, тёмная тема, live reload, якоря и оглавление, горизонтальная прокрутка кода и таблиц, ассоциация, установщик;
- сноски, HTML-подмножество, SVG, удалённые картинки, UIA и печать сюда не входят. Тот же автор оценил их ещё в ≈3–4 месяца;
- **реалистично:** v1 по списку выше — от 3 месяцев одного опытного C++/Win32-разработчика, продукт с доступностью — ≈5–6 месяцев. Уточнить после спайка (§9).

---

## 7. Матрица решений: что выбрать при каких приоритетах

MVP и продукт — оценки из README прототипов.

| Стек | medium, мс | Вид | Что даётся бесплатно | Что писать самим | MVP → продукт | macOS/Linux |
|---|---|---|---|---|---|---|
| **cpp-d2d** (рекомендация) | **59** | 9 | — | выделение, поиск, копирование, UIA, HTML, печать | 6–8 нед. → +3–4 мес. | нет |
| RichEdit, windowless / оконный | 69 / 84 | 7 | у оконного: выделение, копирование RTF, поиск, зум, печать, IME, UIA | вид: эмодзи, скругления, ссылки | 3–5 нед. | нет |
| litehtml (весь документ) | 103 | 9 | вёрстка HTML/CSS, тема через CSS | выделение, поиск, UIA; вёрстка сверхлинейна (3,7 МБ целиком > 30 с) → резать на куски | ≈2–2,5 мес. | частично |
| Blitz (Rust) | 112 | 8 | настоящий CSS (Stylo), AccessKit | поиск, виртуализация, патч winit; beta | 5–7 нед. + поддержка | да |
| Avalonia 12 NativeAOT, Skia CPU | 143 | 8 | контролы, .NET | выделение через блоки; 31 МБ | 4–6 нед. | да |
| Qt 6 Widgets | 149 статич. / 154 DLL | 8,5 | выделение, поиск, IME, UIA, печать, HTML-блоки | оформление вручную (урезанный CSS) | 3–5 нед. → 2–3 мес. | да |
| WinUI 3 (C# AOT) | 324 | 8 | UIA, контролы | вёрстка документа из блоков | 6–8 нед. → 12–14 нед. | нет |
| WebView2 (C++) | 447 (резидент 65) | 9 | всё HTML/CSS/JS, Mermaid, KaTeX | обвязку | 3–4 нед. → +4–6 нед. | нет |

**Как читать:**
- **Главное — «пуля»:** `cpp-d2d`.
- **Главное — быстро выпустить v1:** RichEdit, чей вид хуже, или Qt, который медленнее примерно на 90 мс, но из коробки даёт выделение, поиск, IME и UIA.
- **Нужен macOS/Linux в ближайшие 1–2 года:** решать до начала работы (§8). Движок на DirectWrite непереносим, на macOS его пришлось бы писать заново на CoreText. Варианты:
  - Qt: официальные DLL, 154 мс. Статическая сборка на 149 мс под LGPL требует выдавать объектные файлы или покупать коммерческую лицензию;
  - общее ядро (модель, блочная вёрстка, выделение, поиск) с тонким слоем шейпинга и растра под каждую платформу ([03 §6.1](research/03-gpu-and-crossplatform-native-ui.md)).
- **Предпочтителен Rust** (безопасность памяти при разборе недоверенных файлов; comrak, RaTeX, mmdr и resvg — всё Rust): Rust + windows-rs + DirectWrite по той же архитектуре. По построению скорость та же (§3.7), но **прототипа нет, не измерено**. Если нужен готовый CSS/HTML — Blitz. pulldown-cmark не умеет GFM-автоссылки, поэтому брать comrak.
- **Нужна точная вёрстка Chromium для всех документов:** WebView2 только резидентно. В установившемся режиме 65 мс, первый документ после старта резидента 165–280 мс. Резидент держит ≈150–260 МБ в фоне.

**Не брать:**
- **Electron:** 0,75–0,9 с, 324 МБ.
- **Tauri:** 630 мс.
- **WPF:** 287 мс, и то только с героическими хаками.
- **Sciter:** закрытые исходники, 320 мс, дефекты вёрстки.
- **GPU-first тулкиты** (egui и iced на wgpu, Blitz-GPU): 324–735 мс.
- **egui/iced в быстрой конфигурации:** держится на патчах winit и wgpu, визуал 6,5–7.

---

## 8. Риски и открытые вопросы

1. **Трудоёмкость своего движка.**
   - Сложнее всего: выделение через блоки и ячейки таблиц, поиск в виртуализованном документе, копирование как text/HTML, UIA `TextPattern`, HTML-подмножество.
   - Смягчение: спайк по убыванию риска (§9), UIA заложить в архитектуру сразу, откат на RichEdit/litehtml/Qt по заранее заданному критерию.
2. **Недоверенный ввод.** .md из интернета разбирается без песочницы кодом на C/C++: md4c, WIC, lunasvg, HTML-подмножество. Ссылки уходят в `ShellExecute`. Нужны:
   - белый список схем: `http`, `https`, `mailto` и относительные .md; `file:`, UNC и сторонние протоколы — только с подтверждением;
   - фаззинг парсера, HTML и SVG;
   - `/GS` и CFG в релизе (прототип собран с `/GS-`);
   - настройка для удалённых картинок «всегда / спрашивать / никогда», потому что трекинг-пиксели раскрывают IP.
3. **Кроссплатформенность решить до начала работы.** Если «только Windows», записать это как осознанное решение. Иначе смотреть §7.
4. **Прокрутка на слабых CPU и в 4K не измерена.** На этой машине CPU-путь быстрее аппаратного. Прогнать `--scroll-test` на 2–4-ядерном ноутбуке до выбора бэкенда.
5. **Всё измерено на одной машине с дискретной NVIDIA** (§1, оговорка 6). До окончательного решения прогнать харнесс на ноутбуке только с iGPU: `pwsh -File build.ps1` и `bench.py run` переносимы.
6. **IME.** 59 мс получены с отключённым IME UI-потока; без этого ≈+10 мс. Поле поиска с CJK-вводом — в отдельном потоке. Показ без активации в финальном прогоне не мерился и из Explorer не проверялся.
7. **Резидентный режим, если понадобится.** Возникают вопросы:
   - передача переднего плана (`CoAllowSetForegroundWindow`);
   - обновление exe при живом резиденте;
   - вытеснение рабочего набора после простоя (не измерено);
   - EcoQoS-троттлинг фонового процесса;
   - запасное cloaked-окно привязано к виртуальному рабочему столу и монитору/DPI, где создано;
   - один процесс на все окна — одна точка отказа ([06 §9](research/06-product-ux-landscape.md) советует процесс на окно);
   - автозапуск и «ещё один фоновый процесс» в глазах пользователя.
8. **Поставка.**
   - Установка без MSIX, в HKCU: ProgID, RegisteredApplications, при резиденте ещё COM LocalServer32.
   - Подпись кода, иначе SmartScreen предупреждает при первом запуске.
   - Своё автообновление.
   - MSIX не брать: виртуализация AppData, активация не измерена, произвольный DelegateExecute через манифест, вероятно, не зарегистрировать.
9. **Особенности вашей машины, влияющие на любые приложения:**
   - Windhawk: ≈5 мс CPU на процесс;
   - RTSS: ≈10 мс на первое окно;
   - `TextInputHost.exe` набрал 139 тыс. секунд CPU — вероятно, неисправен. Перезапуск или выход и вход в систему, скорее всего, удешевит первую активацию окон у программ без отключённого IME. Финальный прогон этого не проверял.

---

## 9. Следующий шаг

1. **Окончательное решение принять после спайка**, по заранее заданным критериям.
2. **Спайк на 2–3 недели на базе `bench/protos/cpp-d2d`**, по убыванию риска:
   - выделение через блоки и ячейки таблиц, копирование как text/HTML, поиск с подсветкой в виртуализованном документе;
   - HTML-подмножество из [06 §4.3](research/06-product-ux-landscape.md), SVG-бейджи (lunasvg) и удалённые картинки на 20–30 реальных README со скриншот-сравнением с GitHub;
   - минимальный UIA `TextPattern` с проверкой в Экранном дикторе;
   - поле поиска с IME в отдельном потоке;
   - харнесс и `--scroll-test` в 4K на ноутбуке только с iGPU и слабым CPU;
   - Mica и тёмная тема почти бесплатны — не в приоритете.
3. **Критерий отката:** если выделение или HTML-подмножество выходят за оценку более чем в 1,5 раза — переходим на litehtml (весь документ) или Qt. Если нужен быстрый v1 любой ценой по виду — на RichEdit.
4. **Регрессионный тест скорости.** Ночной прогон на выделенной машине с интерактивной сессией, в одном чередующемся прогоне с эталоном. Критерии:
   - `t_content(fastmd) − t_content(baseline-win32)` ≤ 0 мс на medium и ≤ +15 мс на large;
   - контроль фазы `main → layout_first_viewport`.

---

## Приложение A. Все 47 вариантов (финальный прогон, медиана в мс)

| # | вариант | стек | small | medium | large | p90 med | окно med | CPU мс | commit МБ | дистр. МБ | визуал |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `cpp-d2d-resident` | C++ D2D/DWrite, резидентный процесс (hand-off) | 29 | **28** | 29 | 29.3 | 21.2 | 15.6³ | 1.8³ | 0.4 | 9 |
| 2 | `cpp-d2d` | C++ Win32 + DirectWrite → GDI DIB + md4c | 60 | **59** | 61 | 61.1 | 49.3 | 31.2 | 7.0 | 0.4 | 9 |
| 3 | `cpp-webview2-resident` | WebView2, резидентный процесс (hand-off) | 65 | **65** | 84 | 69.0 | 54.8 | 15.6³ | 1.7³ | 0.52 | 9 |
| 4 | `cpp-richedit-wl-mt` | C++ RichEdit windowless (GDI) + md4c→RTF | 70 | **69** | 72 | 72.5 | 55.8 | 46.9 | 4.5 | 0.26 | 7 |
| 5 | `rust-gpu-egui-softfirst-noime` | egui softfirst + без IME | 76 | **76** | 77 | 82.0 | 68.2 | 46.9 | 10.1 | 7.54 | 7 |
| 6 | `rust-gpu-egui-softfirst` | egui (CPU первый кадр, пропатченный winit) | 78 | **77** | 83 | 94.5 | 70.1 | 62.5 | 10.1 | 7.54 | 7 |
| 7 | `baseline-win32` | Эталон: пустое Win32+GDI окно | 77 | **78** | 78 | 92.5 | 72.3 | 31.2 | 3.2 | 0.15 | — |
| 8 | `cpp-richedit-d2d` | RichEdit windowless через D2D/DirectWrite | 79 | **83** | 86 | 92.7 | 69.9 | 46.9 | 61.2 | 0.26 | 7 |
| 9 | `cpp-richedit` | RichEdit (обычный контрол) + md4c→RTF | 80 | **84** | 86 | 85.6 | 78.3 | 62.5 | 4.3 | 0.25 | 7 |
| 10 | `cpp-d2d-warp` | C++ + Direct2D на WARP (CPU) | 75 | **91** | 92 | 93.2 | 49.8 | 93.8 | 75.8 | 0.4 | 9 |
| 11 | `cpp-richedit-full` | RichEdit оконный, весь документ сразу | 90 | **103** | 998 | 104.3 | 97.4 | 62.5 | 5.1 | 0.25 | 7 |
| 12 | `litehtml-sciter-litehtml` | litehtml + свой D2D/DWrite-контейнер | 90 | **103** | 102 | 104.0 | 65.5 | 101.6 | 87.0 | 1.79 | 9 |
| 13 | `blitz` | Blitz (Stylo+Taffy+Parley) CPU | 111 | **112** | 115 | 120.3 | 108.4 | 101.6 | 27.7 | 8.0 | 8 |
| 14 | `blitz-full` | Blitz, весь документ сразу | 108 | **112** | 2478 | 115.6 | 106.4 | 125.0 | 27.6 | 8.0 | 8 |
| 15 | `rust-gpu-iced-tinyskia` | iced 0.14 (tiny-skia CPU) | 120 | **129** | 183 | 132.6 | 123.4 | 242.2 | 64.5 | 7.78 | 6.5 |
| 16 | `avalonia` | Avalonia 12 NativeAOT (DLL+лаунчер), Skia CPU | 132 | **143** | 143 | 149.7 | 123.5 | 109.4 | 42.3 | 31.26 | 8 |
| 17 | `qt` | Qt 6.12 Widgets (статический, в DLL) | 141 | **149** | 151 | 161.3 | 141.8 | 109.4 | 13.9 | 20.12 | 8.5 |
| 18 | `qt-dll` | Qt 6.12 (официальные DLL) | 150 | **154** | 160 | 159.1 | 148.2 | 109.4 | 68.5 | 26.8 | 8.5 |
| 19 | `qt-full` | Qt, весь документ сразу | 137 | **155** | 721 | 160.0 | 144.8 | 125.0 | 15.0 | 20.12 | 8.5 |
| 20 | `avalonia-exe` | Avalonia 12 NativeAOT exe, Skia CPU | 149 | **163** | 164 | 168.0 | 143.8 | 101.6 | 42.4 | 31.17 | 8 |
| 21 | `litehtml-sciter-litehtml-gpu` | litehtml + D2D HwndRenderTarget (GPU) | 241 | **259** | 260 | 278.7 | 254.5 | 226.6 | 84.8 | 1.79 | 9 |
| 22 | `cpp-d2d-gpu` | C++ + Direct2D на аппаратном D3D11 | 232 | **261** | 258 | 272.8 | 49.8 | 250.0 | 133.9 | 0.4 | 9 |
| 23 | `wpf` | WPF .NET 8 R2R + нативный лаунчер | 295 | **287** | 295 | 295.7 | 278.3 | 429.7 | 109.8 | 1.75 | 8 |
| 24 | `wpf-hw` | WPF, GPU с первого кадра | 291 | **292** | 311 | 313.6 | 282.2 | 484.4 | 135.6 | 1.75 | 8 |
| 25 | `litehtml-sciter-sciter` | Sciter.JS 6 | 319 | **320** | 326 | 326.1 | 305.3 | 234.4 | 137.4 | 18.75 | 7 |
| 26 | `winui3` | WinUI 3 NativeAOT, XAML | 302 | **324** | 330 | 329.2 | 122.0 | 375.0 | 163.6 | 58.08 | 8 |
| 27 | `rust-gpu-egui-wgpu` | egui на wgpu DX12 | 323 | **324** | 324 | 332.3 | 96.6 | 273.4 | 382.1 | 7.54 | 7 |
| 28 | `wpf-apphost` | WPF, штатный apphost | 336 | **333** | 333 | 344.9 | 322.5 | 406.2 | 56.2 | 1.75 | 8 |
| 29 | `avalonia-gpu` | Avalonia 12 NativeAOT, ANGLE/D3D11 | 335 | **344** | 326 | 356.1 | 288.3 | 312.5 | 189.9 | 31.26 | 8 |
| 30 | `winui3-r2r` | WinUI 3 JIT+ReadyToRun | 362 | **384** | 388 | 400.1 | 206.1 | 460.9 | 170.0 | 100.77 | 8 |
| 31 | `cpp-webview2` | C++ + WebView2 (тюнинг) | 429 | **447** | 445 | 454.8 | 81.6 | 789.0 | 150.6 | 0.52 | 9 |
| 32 | `winui3-fluent` | WinUI 3 + Fluent-ресурсы + Mica | 426 | **449** | 449 | 476.6 | 330.3 | 429.7 | 172.1 | 59.37 | 8 |
| 33 | `cpp-webview2-default` | C++ + WebView2 (без тюнинга, GPU-процесс, весь документ) | 445 | **465** | 2105 | 473.8 | 81.3 | 1195.3 | 181.7 | 0.52 | 9 |
| 34 | `rust-webview` | Rust wry + WebView2 | 468 | **473** | 474 | 484.4 | 87.9 | 1234.3 | 183.9 | 2.46 | 9 |
| 35 | `rust-webview-tao` | Rust wry + tao + WebView2 | 470 | **473** | 483 | 506.0 | 199.7 | 1265.6 | 182.5 | 2.46 | 9 |
| 36 | `ext-notepad` | Блокнот (синтаксис md; до окна; восстанавливает вкладки) | 464 | **473** | 714 | 497.0 | 473.4 | 609.4 | 92.2 | — | — |
| 37 | `wpf-plain` | WPF «как в учебнике» | 489 | **487** | 491 | 509.9 | 476.5 | 562.5 | 81.2 | 0.67 | 8 |
| 38 | `winui3-webview2` | WinUI 3 + WebView2 | 479 | **497** | 495 | 519.9 | 131.2 | 1070.3 | 280.1 | 58.08 | 9 |
| 39 | `avalonia-stock` | Avalonia 12 по умолчанию (JIT, Fluent, ANGLE) | 570 | **570** | 574 | 587.7 | 497.5 | 578.1 | 186.4 | 37.47 | 8 |
| 40 | `rust-webview-tauri` | Tauri 2 | 591 | **630** | 668 | 663.7 | 511.8 | 1367.2 | 200.4 | 5.64 | 9 |
| 41 | `blitz-gpu` | Blitz на GPU (vello_hybrid/wgpu) | 742 | **735** | 749 | 750.3 | 727.9 | 656.2 | 236.1 | 10.51 | 8 |
| 42 | `electron` | Electron 44 (тюнинг, упакован) | 727 | **753** | 744 | 757.4 | 627.6 | 523.4 | 93.8 | 323.54 | 9 |
| 43 | `electron-gpu` | Electron 44 с GPU-процессом | 805 | **808** | 809 | 823.2 | 622.1 | 687.5 | 174.4 | 323.54 | 9 |
| 44 | `electron-default` | Electron «типичное приложение» | 853 | **903** | 1391 | 932.4 | 750.4 | 945.3 | 205.9 | 367.59 | 9 |
| 45 | `ext-vscode` | VS Code 1.113 (редактор; до окна с именем файла) | 1158 | **1138** | 1149 | 1183.2 | 1138.4 | 1593.8 | 332.2 | 1264 | — |
| 46 | `ext-void` | Void (ваша ассоциация .md; чистый профиль, онбординг поверх) | 1442 | **1444** | 1441 | 1471.9 | 1444.2 | 1726.5 | 350.8 | 449 | — |
| 47 | `ext-cursor` | Cursor 2.5 (экран логина поверх) | 2338 | **2351** | 2362 | 2409.2 | 2351.2 | 3070.4 | 638.8 | 782 | — |

Как читать столбцы:
- **«окно»** — медиана самоотметки `t_window`, первого отрисованного окна. Для `ext-*` это момент появления окна по наблюдению харнесса.
- **CPU** — суммарное время CPU всех процессов Job Object к моменту `t_content`, с гранулярностью 15,6 мс.
- **commit** — пиковая выделенная память Job Object.
- ³ У `*-resident` CPU и commit относятся только к лаунчеру. Резидент `cpp-d2d` держит ещё ≈20–30 МБ, резидент WebView2 — ≈150–260 МБ.
- **Сбоев в финальном прогоне — 0** из 1 833 запусков. 14 из них простояли 2,1–2,7 с внутри `CreateProcessW` (§1, оговорка 5).

## Приложение B. Где что лежит

**Теория:**

| Отчёт | Тема |
|---|---|
| [01](research/01-native-ui-frameworks.md) | нативные UI-фреймворки |
| [02](research/02-web-and-html-engines.md) | веб- и HTML-движки |
| [03](research/03-gpu-and-crossplatform-native-ui.md) | GPU- и кроссплатформенные тулкиты |
| [04](research/04-markdown-pipeline.md) | конвейер Markdown: парсеры, подсветка, математика, Mermaid |
| [05](research/05-windows-startup-engineering.md) | инженерия запуска Windows |
| [06](research/06-product-ux-landscape.md) | продукт и UX, конкуренты |

Лабораторные эксперименты — в `research/lab/`.

**Практика:**
- `bench/protos/<id>/`: исходники, `build.ps1`, `proto.json`, `README.md` с разбивкой времени и разделом «Аудит».
- Харнесс и протокол: `bench/harness/`, `bench/PROTOCOL.md`.

**Воспроизвести финальный прогон:** `bench/results/final/run_final.cmd`, запускать из обычного терминала или через Проводник. Сводная таблица: `python bench/harness/final_table.py bench/results/final/main.json bench/results/final/res-*.json`.
